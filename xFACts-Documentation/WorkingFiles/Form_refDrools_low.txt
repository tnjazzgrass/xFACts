VERSION 1.0 CLASS
BEGIN
  MultiUse = -1  'True
END
Attribute VB_Name = "Form_refDrools_low"
Attribute VB_GlobalNameSpace = False
Attribute VB_Creatable = True
Attribute VB_PredeclaredId = True
Attribute VB_Exposed = False
Option Compare Database

Private Sub canRefPrdDrl_Click()
    
    DoCmd.Close acForm, "refDrools_low", acSaveNo
    [Forms]![Call REST API].Visible = True
    
End Sub
Private Sub refDrlsLow_Click()
Dim del As Long
Dim dB As Database
Dim rst1 As Recordset
Dim httpReq As New XMLHTTP60
Dim response As String, jBody As String, sql As String
Dim sURL As String, sUser As String, sPass As String, sAuth As String

    Set dB = CurrentDb
    
    sUser = [Forms]![Call REST API]![APIuser]
    sPass = [Forms]![Call REST API]![APIpass]
    sAuth = [Forms]![Call REST API]![authCode]
    
    Select Case [Forms]![Call REST API]![runEnv]

'TEST
    Case 1
        sURL = "http://dm-test-app:8080/dm-rest-services/api/scheduledjobs/REFRESH_DROOLS"

            httpReq.Open "POST", sURL, False, sUser, sPass
            httpReq.setRequestHeader "Content-Type", "application/vnd.fico.dm.v1+json"
            httpReq.setRequestHeader "Authorization", sAuth
            httpReq.sEnd

            response = httpReq.responseText
            
        dB.Execute "INSERT INTO toolUse (toolFunction, toolEnv, toolUser, toolDate) VALUES ('refDrools', 'TEST', '" & Environ("username") & "', Now())"
    
    MsgBox response, vbInformation, "TEST WEBSERVICE RESPONSE"

'STAGE
    Case 2
    '   App
        sURL = "http://stage-app3:8080/dm-rest-services/api/blaze/promoterules"

            httpReq.Open "POST", sURL, False, sUser, sPass
            httpReq.setRequestHeader "Content-Type", "application/vnd.fico.dm.v1+json"
            httpReq.setRequestHeader "Authorization", sAuth
            httpReq.sEnd

            response = "APP3: " & httpReq.responseText

    '   App2
    '    sURL = "http://dm-stage-app2:8080/dm-rest-services/api/scheduledjobs/REFRESH_DROOLS"

    '        httpReq.Open "POST", sURL, False, sUser, sPass
    '        httpReq.setRequestHeader "Content-Type", "application/vnd.fico.dm.v1+json"
    '        httpReq.setRequestHeader "Authorization", sAuth
    '        httpReq.sEnd

    '        response = response & Chr(13) & Chr(10) & "APP2: " & httpReq.responseText

    '   App3
    '    sURL = "http://dm-stage-app3:8080/dm-rest-services/api/scheduledjobs/REFRESH_DROOLS"

    '        httpReq.Open "POST", sURL, False, sUser, sPass
    '        httpReq.setRequestHeader "Content-Type", "application/vnd.fico.dm.v1+json"
    '        httpReq.setRequestHeader "Authorization", sAuth
    '        httpReq.sEnd

    '        response = response & Chr(13) & Chr(10) & "APP3: " & httpReq.responseText
                
            dB.Execute "INSERT INTO toolUse (toolFunction, toolEnv, toolUser, toolDate) VALUES ('refDrools', 'STAGE', '" & Environ("username") & "', Now())"
            
        MsgBox response, vbInformation, "STAGE WEBSERVICE RESPONSE"
        
    End Select
    
End Sub
